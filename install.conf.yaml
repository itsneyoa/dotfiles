- defaults:
    link:
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    ~/.gitconfig:
      force: true
    ~/.config:
      glob: true
      path: config/*
      force: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]
