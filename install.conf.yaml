- defaults:
    link:
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.gitconfig:
    ~/.config:
      force: true
    ~/.nvmrc:

- create:
    - ~/code

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [./install-fisher, Install fisher and all its dependencies]
